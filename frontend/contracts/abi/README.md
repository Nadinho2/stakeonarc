# ABIs consumed by the Next.js app

// VIBE: After `forge build`, copy `out/VibeToken.sol/VibeToken.json` (and VibeStaking) here, **or** replace the `abi` array in these files so they match your Solidity.

The frontend imports from this folder only (bundler scope).
